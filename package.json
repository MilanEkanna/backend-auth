{
  "name": "backend-auth",
  "version": "2.0.0",
  "private": true,
  "description": "Full-stack JWT authentication app — Express backend + React frontend, deployable to Vercel.",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "start:backend": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.1",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "uuid": "^14.0.1"
  }
}

